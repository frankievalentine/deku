[package]
name = "deku-plugin-network"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[lints.rust]
unsafe_code = "allow"

[dependencies]
deku-plugin-sdk = { path = "../../crates/deku-plugin-sdk" }
async-trait = { workspace = true }
tracing = { workspace = true }
