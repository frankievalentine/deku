// Renders Angie config fragments from Handlebars templates.
#![allow(dead_code)]

use anyhow::Result;

pub fn write_app_config(_app_name: &str, _upstreams: &[String]) -> Result<()> {
    // Milestone 2 implementation
    Ok(())
}

pub fn remove_app_config(_app_name: &str) -> Result<()> {
    // Milestone 2 implementation
    Ok(())
}
