// Renders Angie config fragments from Handlebars templates.

use anyhow::Result;

pub fn write_app_config(app_name: &str, upstreams: &[String]) -> Result<()> {
    // Milestone 2 implementation
    Ok(())
}

pub fn remove_app_config(app_name: &str) -> Result<()> {
    // Milestone 2 implementation
    Ok(())
}
