# Base Angie configuration for Deku
# This file is managed by dekud — do not edit manually.
# Per-app config fragments are in /etc/angie/conf.d/deku/

server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    location / {
        return 404;
    }
}
